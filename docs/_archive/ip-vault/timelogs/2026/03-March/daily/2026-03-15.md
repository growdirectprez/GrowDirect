---
type: session
domain: canary
status: active
created: 2026-03-15
updated: 2026-03-19
---
# 2026-03-15

## Session 1 — Onboarding Flow Redesign + Health Check Progress Fix

**Agent:** ALX
**Platform:** Claude Opus — Cowork
**Duration:** ~2h

### Deliverables

| # | Issue | What shipped | Status |
|---|---|---|---|
| 1 | GRO-235 | Welcome page full-frame redesign (scroll-gated consent, compact layout, inline bird, 2-col tiles, settings picker visible) | Done |
| 2 | GRO-235 | Onboarding pipeline: OAuth → /welcome → health check progress → /home (fixed FK violation, undefined var, OAuth redirects) | Done |
| 3 | GRO-235 | Health check progress ring fix — Valkey state now updates immediately on stage transitions (was stuck at 45% for 120s) | Done |
| 4 | GRO-235 | TSP load stage 30s zero-patience bail-out (was 120s timeout when 0 transactions) | Done |
| 5 | GRO-234 | Logged TSP permission bug — sub1_seal denied on evidence_records post DB consolidation | Bug filed |

### Key Decisions
- AWS for production (not self-hosted Mac Studio + Tesla Powerwall)
- M5 MacBook Pro = headless dev server, Mac Mini = daily desktop + QA workstation
- Full-frame takeover pattern for all pre-dashboard onboarding screens (no sidebar/topbar/tabbar)

### Blockers
- GRO-234: TSP subscribers all unhealthy — `permission denied for table evidence_records`. Blocks all real data flow. Must fix DB grants before any data testing.
- ALX memory_store HTTP endpoint returns 401 (auth required) — had to use container exec workaround. Environment issue to sort out.

---

## Session 2 — EJ Spine Receipt Popup + Drill Path Fixes

**Agent:** ALX
**Platform:** Claude Opus — Cowork
**Duration:** ~2h

### Deliverables

| # | Issue | What shipped | Status |
|---|---|---|---|
| 1 | GRO-236 | `_build_txn_spine()` — full receipt JSON assembler (header, line items, tenders, security, context) | Done |
| 2 | GRO-236 | `_get_spine_data()` — DB fetch with employee name lookup from employees table | Done |
| 3 | GRO-236 | `/txn/<txn_id>/spine` JSON endpoint | Done |
| 4 | GRO-236 | Receipt modal in owl.html — open on card tap, close via X/ESC/backdrop | Done |
| 5 | GRO-236 | 12 unit tests for spine (9 build, 3 DB fetch) — all green | Done |
| 6 | GRO-236 | CSP nonce fix — close button addEventListener was silently blocked | Done |
| 7 | GRO-236 | Drill path fixes — qualified field names, DATE() truncation, scope_count | Done |

### Key Decisions
- Employee name not on Transaction model — requires join to app.employees table via employee_id
- Receipt modal uses data-txn-id attributes + addEventListener (not inline onclick) for CSP compliance
- All new `<script>` blocks in templates must include `nonce="{{ csp_nonce() }}"` — CSP silently blocks without it

### Blockers
- All GRO-236 changes still uncommitted — need commit + PR next session
- ALX memory_store still 401 — memories captured in Linear comment instead
