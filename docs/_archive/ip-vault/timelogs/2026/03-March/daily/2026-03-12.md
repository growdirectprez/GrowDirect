---
type: session
domain: canary
status: active
created: 2026-03-12
updated: 2026-03-19
---
# 2026-03-12

## Session 1 — GRO-209 DB Consolidation (Completion)

**Agent:** ALX
**Platform:** Claude Opus — Cowork
**Duration:** ~1.5h (continuation session)

### Deliverables

| # | Issue | What shipped | Status |
|---|---|---|---|
| 1 | GRO-209 | Task 21 final cleanup — schema-qualified raw SQL in devops_monitor, scenario_runner, chirp_lab, data_factory, merchant_simulator | Done |
| 2 | GRO-209 | config.py fallback URL fixed (canary_app → canary) | Done |
| 3 | GRO-209 | scenario_runner consolidated from 2 DB connections to 1 | Done |
| 4 | GRO-209 | Caught and fixed undefined `db_name` variable bug in devops_monitor | Done |
| 5 | GRO-209 | Logged test suite memory bloat as new Linear issue | Done |

### Key Decisions
- All remaining `canary_app`/`canary_sales`/`canary_metrics` in comments/docstrings left as-is — documentation artifacts, no runtime impact, can clean incrementally
- Raw SQL schema-qualification is critical — ORM handles it via MetaData but text() queries need explicit `schema.table` prefixes
- Test suite bloat logged as separate issue rather than fixing inline (scope control)

### Blockers
- ALX pgvector memory endpoints return 401 — known issue, used file-based memory instead
- Full unit test suite times out / OOMs (~2.7GB) — logged as GRO-209 in Linear

## Session 2 — GRO-213/214 Onboarding Fix + CSS Revert

**Agent:** ALX
**Platform:** Claude Opus — Cowork
**Duration:** ~2h

### Deliverables

| # | Issue | What shipped | Status |
|---|---|---|---|
| 1 | GRO-213 | Fixed merchant ID mismatch in onboarding gate + welcome routes — redirect loop resolved | Done |
| 2 | GRO-213 | Welcome → /home redirect (was /chirps) | Done |
| 3 | GRO-213 | Restored CANARY_STUB_AUTO_LOGIN=1 in .env | Done |
| 4 | GRO-214 | Reverted CSS file merge (3→1) — restored canary.css + mobile-panels.css + responsive-shell.css | Done |
| 5 | GRO-214 | Theme tokens and template tokenization retained | Done |
| 6 | GRO-214 | base_app.html restored to 4 CSS file loads | Done |

### Key Decisions
- Merchant ID mismatch root cause: `g.merchant_id` is internal UUID from .env, but `merchant_sources` stores Square ID. All queries need fallback resolution via merchants table.
- CSS file merge abandoned — caused layout regressions (topbar hidden, breakpoints wrong). Consolidation needs per-page visual testing if attempted again.
- Docker `restart` does NOT reload `.env` — must use `--force-recreate`.

### Blockers
- GRO-214 still In Progress — topbar polish, bird asset, type scale, transitions remain from original spec
- Desktop topbar hidden by CSS breakpoint (>=1024px) — Live button not accessible on wide screens
